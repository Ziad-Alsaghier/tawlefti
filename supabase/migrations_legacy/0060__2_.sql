CREATE TABLE temp_full_blends (
    blend_code TEXT,
    coffee_name TEXT,
    percentage INTEGER,
    crema INTEGER,
    body INTEGER,
    acidity INTEGER,
    bitterness INTEGER,
    flavor INTEGER,
    notes TEXT,
    roast_profile TEXT
);